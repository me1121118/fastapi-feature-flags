[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fastapi-feature-flags"
version = "0.1.0"
description = "Lightweight feature flags dependency and route guard for FastAPI applications"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [
    { name = "me1121118" }
]
keywords = ["fastapi", "python", "utility", "tools"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = ["fastapi>=0.100.0"]

[project.urls]
Homepage = "https://github.com/me1121118/fastapi-feature-flags"
Repository = "https://github.com/me1121118/fastapi-feature-flags"
